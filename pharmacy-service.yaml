spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pharmacy_db
    username: postgres
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8081

eureka:
  client:
    service-url:
      default: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true

resilience4j:
  circuitbreaker:
    instances:
      warehouseCB: 
        failureRateThreshold: 50
        waitDurationInOpenState: 2000
app:
  branchName: "Nha thuoc so 1"
  hotline: "0961761326"
  vat: 10
